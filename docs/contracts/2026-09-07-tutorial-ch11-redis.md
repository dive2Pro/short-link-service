# 需求合约：补充 TUTORIAL 第 11 章教程内容

- 日期：2026-09-07
- 状态：已确认，已交付

## 目标

把 `docs/TUTORIAL.zh-CN.md` 第 11 章从"4 行提纲"扩充为与第 0-10 章同等颗粒度的完整教程内容，覆盖 Redis 替换、超时、降级、穿透/击穿/雪崩。

## 交付物

`docs/TUTORIAL.zh-CN.md` 第 11 章小节扩充（不新建文件）。

## 范围

- 只改 `docs/TUTORIAL.zh-CN.md` 第 11 章；不动 WAR-GAME-LAB / ARCHITECTURE-LAB（实验 3/4/5 已覆盖"怎么验证"，教程只写"怎么做+验收命令"）；不动用户 src 代码
- 延续全书体例：目标 / 你会学到 / 手敲顺序 / 约束 / 验收（含可执行命令）/ 思考；不出完整类代码（用户选定"同体例扩充"）

## 基线

- 用户代码停在"TTL 内存版 + cache-aside 已接入"（`ShortLinkService.findByCode`）
- pom.xml 无 Redis 依赖；compose.yml 无 Redis 服务；application.properties 无 Redis 配置

## 真实链路

教程为权威学习路径；验收命令基于 compose 服务名 `redis`（与既有 compose 模式一致，用户实现时以自己的 compose 为准）。

## 验证方式

扩充内容与第 0-10 章体例逐段对齐；验收命令全部为可复制执行形式；与 WAR-GAME-LAB 实验 3/4/5 互补不重复（结尾显式衔接）。

## 已确认项

- 深度选"同体例扩充"（用户 2026-09-07 选定）

## 已核实项

- 第 11 章原状仅 5 行（目标 + 约束）
- 第 1-10 章体例为：目标 / 你会学到 / 手敲顺序或建议 / 约束 / 验收 / 思考
- 项目现状：`LinkLookupCache` + `InMemoryLinkLookupCache`（TTL 5 分钟）已存在，cache-aside 已接入 Service

## 推断项

- 教程风格延续"只给目标、约束与验收，不给完整代码"——保留少量可复制的验收命令与配置键，不出完整类代码（与用户选定的"同体例扩充"一致）

## 待确认项

无

## 当前状态

已交付：第 11 章扩充为完整教程节（目标 / 为什么换 Redis / 你会学到 / 手敲顺序 4 步 / 约束 4 条 / 验收命令块 / 三故障各含注入方式、防线、验收信号 / 思考题 / 衔接 WAR-GAME-LAB 实验 3）。原第 12、13 章未动。
